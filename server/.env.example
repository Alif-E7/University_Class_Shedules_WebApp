PORT=5000

# PostgreSQL — university_schedule
DB_HOST=localhost
DB_PORT=5432
DB_NAME=university_schedule
DB_USER=postgres
DB_PASSWORD=your_password_here

# Optional: use instead of individual DB_* vars above
# DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/university_schedule

JWT_SECRET=change-this-to-a-long-random-string-in-production
ADMIN_EMAIL=admin@university.edu
ADMIN_PASSWORD=admin123
